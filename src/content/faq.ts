export const faq = Array.from({ length: 11 }, (_, index) => ({
  id: `faq-${index + 1}`,
  question: `Placeholder question ${index + 1}`,
  answer: "Placeholder answer for the phased FAQ build.",
}));
